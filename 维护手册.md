# Blooming Future Web 维护手册

## 架构边界

- 纯 HTML、CSS、JavaScript；无框架、build tool、npm、数据库或外部 CDN。
- `content/*.md` 是文案 source of truth，`index.html` 是同步后的渲染文件。
- `main` 是唯一生产分支，推送后由 Cloudflare Pages 自动部署。
- 品牌五色、品牌字体、双语并列和联系信息受 `CLAUDE.md` 红线保护。

## 常规健康检查

```bash
bash .loopwork/hooks/verify.sh
git status --short
```

浏览器至少检查 `1440×900`、`1024×768`、`768×1024`、`390×844`：横向溢出为 0、图片完整、Hero 有画面、控制台无 warning/error。

## 常见故障

**本地打不开**：确认命令在仓库根目录运行，端口未被占用；换端口时使用 `python3 -m http.server 8017`。

**线上仍是旧样式**：确认 `index.html` 的 CSS/JS 查询版本已更新、远端 `main` 已包含目标 commit，并查看 Cloudflare Pages 部署状态。不要用 CDN 或临时外链绕过缓存。

**图片不显示**：核对 `assets/img/` 文件名大小写、HTML 路径、固有尺寸与 `loading` 属性。真实素材来源见 `docs/v2-release-report.md`。

**动画异常**：先确认 Canvas `data-fps`、`data-dpr`、`data-paused`；移动端应为 30fps 并关闭 code/connection 层，reduced-motion 应显示静态帧。

## 回滚

不允许 force push。定位需要撤销的发布 commit 后：

```bash
git revert <release-commit>
git push origin main
```

Cloudflare Pages 会自动部署反向提交。回滚后重新跑完整测试并检查生产首页。

## 三句求助话术

1. 「继续我的 BloomingFuture-web 项目」
2. 「我的 BloomingFuture-web 打不开了，帮我看看」
3. 「给 BloomingFuture-web 加个 XX 功能」
