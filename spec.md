# BloomingFuture-web v2.0 · 规格（待批准）

## F1 首屏 AI 知识网络

验收：
1. 打开首页，第一屏能同时看清现有 Logo、花开远方品牌名、中英定位与动态知识网络。
2. 汉字、AI token、代码片段和连接线有明确远近层次，大 token 与连接线不会穿过标题和正文。
3. 桌面移动鼠标时，附近节点产生轻微关系变化；移出后平稳复位，不影响点击导航。
4. 页面切到后台或首屏滚出视口后动画暂停；回来后继续，不突然跳帧。
5. 系统选择“减少动态”时只显示静态知识网络，不持续播放。

## F2 真实教育证据

验收：
1. About 能看到天津楼宇、课堂和教学活动三类真实图片，图片均来自仓库现有 `far1999.com` 素材。
2. 页面明确呈现 1999、2015、2023、现在四个时间节点，让访客能扫读机构发展路径。
3. 四个集团数字仍可读且只计数一次，图片出现时不会造成文字跳动。
4. 页面不展示或暗示新加坡总部实景图。

## F3 AI 教育表达

验收：
1. AI Education 能看见“教材 → 知识点 → 启发式问题 → 学生表达 → 教师反馈”的学习过程。
2. 该过程明确是方法可视化，不伪装成已经上线的产品后台或聊天机器人。
3. 四项 AI 教育能力在桌面为整齐 2×2，在手机为清晰单栏，标题和双语描述不被截断。
4. 五组真实教材图片可查看；手机上能手动横向浏览且不会带动整页横向滚动。

## F4 九个十分钟

验收：
1. 桌面能看到严格 3×3 的九张卡，前三张红、中三张橙、后三张绿。
2. 每张卡都完整显示编号、中英标题和中英说明，卡高和文字基线整齐。
3. 手机上九项变为紧凑可读布局，不出现横向溢出或文字遮挡。

## F5 资质与可信度

验收：
1. 商务部和 ISO 真实低清证书以接近原始清晰度的小窗展示，不被拉成大幅模糊图。
2. 每张证书旁有可读的双语事实说明；缺少图片的资质只用文字，不生成假证书。
3. 所有证据图片加载失败时仍保留文字事实，版面不坍塌。

## F6 Singapore 降权

验收：
1. Singapore 是一条明显短于当前版本的全球连接横带，不再与天津总部争夺主视觉。
2. 五个 tag 在宽屏同一行；窄屏换行后每一行仍均匀填满。
3. 新加坡实体、地址与联系方式继续可查，但不出现实景图片。

## F7 理念与小 A

验收：
1. 创始理念仍保持中英并列，访客不需要穿过悬空图片才能继续阅读正文。
2. 小 A 使用现有 `mascot-elephant.png`，在理念 section 最底部独立横带中显示。
3. 横带上下留白和分隔线清楚，小 A 不做持续跳动或漂浮动画。

## F8 联系方式

验收：
1. Contact 首先突出天津总部实景、地址和两个电话，新加坡信息作为次级栏。
2. 天津地址保持“天津市津南区·启迪科技园 35 号楼”，所有电话、邮箱、营业时间保持现值。
3. Departments 作为两栏下方的全宽信息行，不留空邮箱占位。
4. 手机上顺序为天津、新加坡、部门，电话和邮箱可直接点击。

## F9 响应式、性能与无障碍

验收：
1. 在 1440、1024、768、390 px 宽度均无横向溢出、无文字与图像重叠。
2. 桌面动画以 60fps 为目标；手机明确限制为 30fps 并关闭代码片段、连接线和 pointer 交互。
3. Canvas DPR 不超过 2，所有图片本地加载，页面不产生任何 CDN 或追踪请求。
4. `index.html` 小于 80 KB、CSS 小于 40 KB、JS 小于 20 KB。
5. 键盘可使用导航，图片有准确 alt，Canvas 不被读屏重复朗读。

## 边界（明确不做）

- 不换 Logo，不生成或重绘任何照片、证书、教材和吉祥物。
- 不增加新加坡实景图、报名表、登录、CMS、后台或真实 AI 服务。
- 不引入框架、build tool、npm、CDN、analytics 或第三方动画库。
- 不修改品牌五色、字体、中英并列策略和既有联系信息。
