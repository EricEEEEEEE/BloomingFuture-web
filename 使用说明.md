# Blooming Future Web 使用说明

## 访问网站

- 生产站：<https://bloomingfuture.io>
- 页面为中英并列单页站，顶部导航可直达集团、AI 课程、新加坡、理念和联系区。

## 本地启动

在仓库根目录运行：

```bash
python3 -m http.server 8016
```

浏览器打开 <http://127.0.0.1:8016>。项目没有 build step，也不需要安装 npm 依赖。

停止本地服务：回到运行命令的终端，按 `Ctrl+C`。

## 日常修改

1. 文案先改 `content/<section>.md`，再同步到 `index.html` 对应 section，并更新同步日期。
2. 样式改 `assets/css/main.css`，只使用 `CLAUDE.md` 定义的品牌 token。
3. 交互改 `assets/js/main.js`，保持 Canvas DPR 不超过 2，并保留移动端和 reduced-motion 降级。
4. 图片放在 `assets/img/`，必须使用真实、可追溯素材，并写明固有 `width`、`height` 和 `alt`。

## 验收与上线

完整验收：

```bash
bash .loopwork/hooks/verify.sh
```

测试全绿后提交到 `main` 并执行：

```bash
git push origin main
```

Cloudflare Pages 会自动部署。发布后检查生产首页、四档响应式、图片加载和浏览器控制台。
