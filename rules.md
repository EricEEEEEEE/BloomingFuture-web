# 项目规矩（碰这些之前必须先问我）

1. 不删除任何既有文件，也不覆盖用户未提交的改动。
2. 不花任何钱，不开通付费服务。
3. 不对外发布、部署或 `git push`，除非用户明确批准该动作。
4. 密钥、密码和私人凭据永远不写进代码或日志。
5. Stage 4 的验收脚本写好并确认失败后，实现期间不许修改考题来迁就代码。
6. 每完成一条 `tasks.md` 任务立刻验证并独立 commit。
7. 严格遵守 `CLAUDE.md` 全部 Red Lines，不引入框架、build tool、npm、CDN 或追踪 SDK。
8. 页面图片只能使用现有真实素材或用户后来明确提供的真实素材，不使用 AI 生成图替代。
9. 保留当前 Logo 与小 A 吉祥物，不做高清替换、重绘或风格化改造。
10. 不虚构证书、新加坡实景、产品能力、运营数据或客户案例。
11. 天津地址固定为“天津市津南区·启迪科技园 35 号楼”；其他联系信息未经明确指令不改。
12. 文案改动必须先写入 `content/*.md`，再同步到 `index.html` 并更新同步日期。
13. `main` 是生产分支；不得 force push，发布前必须完成全套 verify 和浏览器验收。
